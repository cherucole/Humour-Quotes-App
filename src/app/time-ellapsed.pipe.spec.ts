import { TimeEllapsedPipe } from './time-ellapsed.pipe';

describe('TimeEllapsedPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeEllapsedPipe();
    expect(pipe).toBeTruthy();
  });
});
